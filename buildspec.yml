version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
        - npm install
        - npm install -g grunt-cli
        - grunt package
  build:
    commands:
        grunt lambda_package
artifacts:
  files:
      - ./dist/github-lambda-demo_latest.zip
  #discard-paths: yes
  #base-directory: location
